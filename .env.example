# -------------------------------------------------------------
# Second Brain RAG — Environment Variables
# Copy this file to .env and fill in all values before running.
# NEVER commit .env to version control.
# -------------------------------------------------------------

# --- Backend API protection (choose a strong random string) ---
API_KEY=change-me-to-a-strong-secret

# --- Google Gemini API key (https://aistudio.google.com/app/apikey) ---
GOOGLE_API_KEY=your_google_api_key_here

# --- Graph Database (Neo4j) ---
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=change-me-strong-neo4j-password

# --- Redis Cache ---
REDIS_URL=redis://redis:6379/0
REDIS_PASSWORD=change-me-strong-redis-password
CACHE_BACKEND=redis

# --- ChromaDB ---
CHROMA_HOST=chroma
CHROMA_PORT=8001
CHROMA_AUTH_TOKEN=change-me-strong-chroma-token

# --- BM25 persistence path (inside the container) ---
BM25_DATA_DIR=/app/bm25_data

# --- RAG tuning ---
CHUNK_SIZE=2000
CHUNK_OVERLAP=200
# Cross-encoder scores range roughly -10 to +10.
# -5 keeps moderately relevant results; -11 keeps everything (useful for small corpora).
RERANK_THRESHOLD=-5.0
MAX_FILE_SIZE_MB=10

# --- CORS (comma-separated origins for the backend) ---
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80
